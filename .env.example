# Production compose configuration. Generate a hex password with: openssl rand -hex 32
DATABASE_PASSWORD=replace-with-a-random-hex-password
PUBLIC_ORIGIN=https://games.example.com
SITE_ADDRESS=games.example.com
# Reserved for the moderator created by the createAdmin CLI; public signup cannot claim it.
ADMIN_USERNAMES=your_username
